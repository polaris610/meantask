'use strict';

// Configuring the Books module
angular.module('books')

	//.run(['Menus',
	//	function(Menus) {
	//		// Set top bar menu items
	//		Menus.addMenuItem('topbar', 'Books', 'books', 'dropdown', '/books(/create)?', false, ['editor']);
	//		Menus.addSubMenuItem('topbar', 'books', 'List books', 'books');
	//		Menus.addSubMenuItem('topbar', 'books', 'New book', 'books/create');
	//	}])
;
